package 核心.内容;

import mindustry.content.Items;

import static 核心.标签.标签类型存储类.清空;
import static 核心.标签.核心标签.add;
import static 核心.标签.核心标签.get;
import static 核心.标签.标签类型存储类.添加标签;

public class 标签内容 {
    //TODO 创建//设置  分开？
    public static void 创建(){
        add("空");
        add("jk");




















    }
    public static void 设置(){
        清空();
        添加标签(Items.copper, get("空"));
















    }


}
